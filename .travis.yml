language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - yarn install
  - yarn test
deploy:
  provider: script
  script: bash ./deploy_to_aws.sh
  on:
    branch: develop
